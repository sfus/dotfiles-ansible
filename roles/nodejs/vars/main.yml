---
# vars file for nodejs
nodejs_volta_dir: "{{ ansible_env.HOME }}/.volta"

# Pin the major version only; Volta tracks the latest patch on install.
nodejs_node_version: "24"

nodejs_global_packages:
  - name: pnpm
  - name: prettier
  - name: eslint
